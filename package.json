{
  "name": "dealshub-miniapp",
  "version": "2.0.0",
  "description": "SmartSkidka.ru — affiliate deals mini-app (v2 React)",
  "private": true,
  "scripts": {
    "test": "node tests/app.test.js",
    "lint": "eslint scripts/ tests/ api/",
    "lint:fix": "eslint scripts/ tests/ api/ --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "node scripts/generate-sitemap.js",
    "update-feed": "node scripts/update-feed.js",
    "generate-items": "echo 'v2: item pages generated during v2 build'",
    "validate-meta": "node scripts/validate-meta.js",
    "validate-jsonld": "node scripts/validate-jsonld.js"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^10.1.8",
    "jsdom": "^29.1.1",
    "license-checker": "^25.0.1",
    "prettier": "^3.9.4",
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
